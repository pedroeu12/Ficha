<resources>
    <!-- Parchment window background so launch never flashes white before Compose draws. -->
    <style name="Theme.Ficha" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/parchment_page</item>
        <item name="android:colorBackground">@color/parchment_page</item>
        <item name="android:statusBarColor">@color/crimson_deep</item>
        <item name="android:navigationBarColor">@color/parchment_page</item>
    </style>
</resources>
